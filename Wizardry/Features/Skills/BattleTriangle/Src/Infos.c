#include "gbafe-chax.h"

// icons
extern const u16 SI_SwordBreaker[], SI_AxeBreaker[], SI_LanceBreaker[], SI_BowBreaker[], SI_TomeBreaker[];

const struct SkillInfo SkillInfo_SwordBreaker = {
	
	.msg_desc = ENUM_msg_SD_SwordBreaker,
	// .msg_name = ENUM_msg_SN_SwordBreaker,
	.icon = SI_SwordBreaker,
	
};


const struct SkillInfo SkillInfo_AxeBreaker = {
	
	.msg_desc = ENUM_msg_SD_AxeBreaker,
	// .msg_name = ENUM_msg_SN_AxeBreaker,
	.icon = SI_AxeBreaker,
	
};


const struct SkillInfo SkillInfo_LanceBreaker = {
	
	.msg_desc = ENUM_msg_SD_LanceBreaker,
	// .msg_name = ENUM_msg_SN_LanceBreaker,
	.icon = SI_LanceBreaker,
	
};


const struct SkillInfo SkillInfo_BowBreaker = {
	
	.msg_desc = ENUM_msg_SD_BowBreaker,
	// .msg_name = ENUM_msg_SN_BowBreaker,
	.icon = SI_BowBreaker,
	
};


const struct SkillInfo SkillInfo_TomeBreaker = {
	
	.msg_desc = ENUM_msg_SD_TomeBreaker,
	// .msg_name = ENUM_msg_SN_TomeBreaker,
	.icon = SI_TomeBreaker, 
	
};


const struct SkillInfo SkillInfo_FistBreaker = {
	
	.msg_desc = ENUM_msg_SD_FistBreaker,
	// .msg_name = ENUM_msg_SN_FistBreaker,
	.icon = 0, // W.I.P.
	
};



